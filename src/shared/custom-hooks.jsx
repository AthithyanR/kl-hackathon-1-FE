/* eslint-disable function-paren-newline */
// import { useQuery } from '@tanstack/react-query';

// export const customUseQuery = (identifier, handler) => useQuery(
//   Array.isArray(identifier) ? identifier : [identifier], async () => {
//     const resp = await handler();
//     return resp.data;
//   });

// export default {};
